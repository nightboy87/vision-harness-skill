# Workflow Diagram Example

Place a process or flowchart image here as `sample_workflow.png`, then run:

```bash
python ../../tools/visual_extract.py sample_workflow.png --task workflow_to_sop --out output
```
