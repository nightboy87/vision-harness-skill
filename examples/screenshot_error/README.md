# Screenshot Error Example

Place an error screenshot here as `sample_error.png`, then run:

```bash
python ../../tools/visual_extract.py sample_error.png --task screenshot_diagnosis --out output
```
